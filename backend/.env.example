APP_NAME=LoseWeight
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://100.89.150.50:8044
APP_LOCALE=en
APP_FALLBACK_LOCALE=en

FRONTEND_URL=http://100.89.150.50:3044

LOG_CHANNEL=stack
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=loseweight
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
CACHE_STORE=database

SANCTUM_STATEFUL_DOMAINS=100.89.150.50:3044,localhost:3044

# Kimlik.az OAuth
WALLET_CLIENT_ID=
WALLET_CLIENT_SECRET=
WALLET_API_URL=http://100.89.150.50:8011/api
WALLET_URL=http://100.89.150.50:3011

MAIL_MAILER=log
MAIL_FROM_ADDRESS="hello@loseweight.net"
MAIL_FROM_NAME="${APP_NAME}"

# AI parsing. Server-side only, and the API is the only place it belongs: it
# owns the durable cache and the spend ledger, both of which must survive a
# restart. Leave blank to run parsing through the deterministic fallback.
ANTHROPIC_API_KEY=
AI_MODEL=claude-haiku-4-5-20251001

# Optional. Live USDA FoodData Central lookups.
USDA_API_KEY=
